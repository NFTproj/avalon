'use client'

// Exemplo de arquivo:  components/Web3Connect.tsx
export default function Web3Connect() {
  return (
    /* você pode estilizar com tailwind, wrapper, etc. */
    <div className="inline-block">
      <appkit-button />
    </div>
  )
}
